package com.pauls.lorebound.ui.theme

import androidx.compose.ui.graphics.Color

// ═══════════════════════════════════════════════════════
// MINIMAL VOID — True black, white text, orange accent
// ═══════════════════════════════════════════════════════

// Core palette — pure blacks
val BrutalBlack = Color(0xFF000000)
val BrutalDarkSurface = Color(0xFF050505)
val BrutalSurfaceVariant = Color(0xFF0A0A0A)
val BrutalWhite = Color(0xFFFFFFFF)
val BrutalDimWhite = Color(0xFFBBBBBB)
val BrutalMuted = Color(0xFF606060)
val BrutalSubtle = Color(0xFF1A1A1A)

// Accent — olive green
val BrutalAccent = Color(0xFF8B9A3C)
val BrutalAccentDim = Color(0xFF6B7A2C)

// Trait colors — desaturated to fit void aesthetic
val TraitExploration = Color(0xFFFF9100)   // Orange
val TraitIntelligence = Color(0xFF64B5F6)  // Soft blue
val TraitCharisma = Color(0xFFEF5350)      // Soft red-pink
val TraitCreativity = Color(0xFFBA68C8)    // Soft purple
val TraitStrength = Color(0xFFE57373)      // Soft red
val TraitCourage = Color(0xFF81C784)       // Soft green

// Semantic
val QuestGreen = Color(0xFF81C784)
val XpPurple = Color(0xFFFFAB40)
val DangerRed = Color(0xFFFF5252)
val LoreBlue = Color(0xFF90CAF9)

// Legacy convenience aliases (used across screens)
val GoldLight = BrutalAccent
val GoldDark = BrutalAccent
val Amber = BrutalAccent

// Text
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = BrutalMuted
val TextOnGold = BrutalBlack

// Rank tier colors
val RankBronze = Color(0xFFCD7F32)
val RankSilver = Color(0xFFC0C0C0)
val RankGold = Color(0xFFFFD700)
val RankPlatinum = Color(0xFFE5E4E2)
val RankDiamond = Color(0xFFB9F2FF)
